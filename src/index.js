import React from "react";
import ReactDOM from "react-dom/client";
import AirPollutionReport from "./components/AirPollutionReport";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <AirPollutionReport />
  </React.StrictMode>
);
